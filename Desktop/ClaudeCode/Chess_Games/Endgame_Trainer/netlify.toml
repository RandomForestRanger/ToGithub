[build]
  publish = "."

# No build command — pure HTML/CSS/JS, no build step.
# Serve locally with: python3 -m http.server 8080
# (Web Workers require HTTP; opening index.html as a file:// URL will not work)
